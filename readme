This is fastAPI CRUD project developed for Kendo's initial demo.

uvicorn main:app --reload --host 192.168.0.162 --port 8000

GITHUB - initial info
==================
create a new repository on the command line
echo "# fastCRUD" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mdmubeenkhan/fastCRUD.git
git push -u origin main


push an existing repository from the command line
git remote add origin https://github.com/mdmubeenkhan/fastCRUD.git
git branch -M main
git push -u origin main

